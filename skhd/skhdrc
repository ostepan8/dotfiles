# Opt+Space → open a new Ghostty window
# Routes through ~/Applications/OpenGhostty.app (compiled AppleScript that
# launches Ghostty if dead, sends Cmd+N if already running)
alt - space : open -a OpenGhostty

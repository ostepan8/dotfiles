# Aerospace config — tiling window manager for macOS.
# Docs: https://github.com/nikitabobko/AeroSpace
#
# Known conflicts with the tmux config in this repo:
#   - Alt+1-9    used by tmux for window switching  → Aerospace takes priority (use `prefix + N` in tmux)
#   - Alt+h/j/k/l used by tmux for pane switching   → Aerospace takes priority (use Ctrl+h/j/k/l via vim-tmux-navigator)
# If you prefer to keep tmux's Alt bindings, rebind Aerospace to cmd-alt-* below.

start-at-login = true

# Normalize container structure — avoids weird nested layouts after windows move.
enable-normalization-flatten-containers = true
enable-normalization-opposite-orientation-for-nested-containers = true

# Accordion layout stays mostly-collapsed (better for stacks of code editors)
accordion-padding = 30

# Auto-pick horizontal vs vertical split based on window dimensions
default-root-container-layout = 'tiles'
default-root-container-orientation = 'auto'

# Don't let windows animate off-screen when switching workspaces
on-focused-monitor-changed = ['move-mouse monitor-lazy-center']

# Notify sketchybar on workspace change so the workspace indicator highlights correctly
exec-on-workspace-change = ['/bin/bash', '-c',
    'sketchybar --trigger aerospace_workspace_change FOCUSED_WORKSPACE=$AEROSPACE_FOCUSED_WORKSPACE'
]

# ==========================
# Mouse / key interaction
# ==========================
automatically-unhide-macos-hidden-apps = false

# Pin workspaces 9 and 10 to the two external monitors.
# Aerospace quirk: the 'external' / 'secondary' keywords don't match HP
# displays on this machine, but regex does.
[workspace-to-monitor-force-assignment]
9  = '^HP 327se \(1\)$'
10 = '^HP 327se \(2\)$'

[key-mapping]
preset = 'qwerty'

# ==========================
# Gaps between windows + from screen edge
# ==========================
[gaps]
inner.horizontal = 8
inner.vertical   = 8
outer.left       = 8
outer.bottom     = 8
outer.right      = 8
# Top gap: 40px on whichever monitor hosts sketchybar, 8px on monitors
# without a bar (so windows tile edge-to-edge for more screen real estate).
# - 'built-in'   : MacBook display (sketchybar pinned there on the laptop)
# - 'ED340CU S3' : Mac Studio's main display (no built-in monitor on a Studio)
outer.top        = [{ monitor.'built-in' = 40 }, { monitor.'ED340CU S3' = 40 }, 8]

# ==========================
# Main mode bindings
# ==========================
[mode.main.binding]

# --- Focus (vim-style) ---
alt-h = 'focus left'
alt-j = 'focus down'
alt-k = 'focus up'
alt-l = 'focus right'

# --- Move window in current workspace ---
alt-shift-h = 'move left'
alt-shift-j = 'move down'
alt-shift-k = 'move up'
alt-shift-l = 'move right'

# --- Resize focused window ---
alt-minus = 'resize smart -50'
alt-equal = 'resize smart +50'
alt-shift-equal = 'balance-sizes'

# --- Layout tweaks ---
alt-slash = 'layout tiles horizontal vertical'   # toggle tile orientation
alt-comma = 'layout accordion horizontal vertical' # stack view
alt-f     = 'fullscreen'
alt-shift-space = 'layout floating tiling'        # toggle float/tile

# --- Workspaces (jump to) ---
# Chaining 'move-mouse monitor-lazy-center' pulls the cursor onto the new
# workspace's monitor — needed because on-focused-monitor-changed doesn't
# always fire reliably when switching workspaces across monitors.
alt-1 = ['workspace 1',  'move-mouse monitor-lazy-center']
alt-2 = ['workspace 2',  'move-mouse monitor-lazy-center']
alt-3 = ['workspace 3',  'move-mouse monitor-lazy-center']
alt-4 = ['workspace 4',  'move-mouse monitor-lazy-center']
alt-5 = ['workspace 5',  'move-mouse monitor-lazy-center']
alt-6 = ['workspace 6',  'move-mouse monitor-lazy-center']
alt-7 = ['workspace 7',  'move-mouse monitor-lazy-center']
alt-8 = ['workspace 8',  'move-mouse monitor-lazy-center']
alt-9 = ['workspace 9',  'move-mouse monitor-lazy-center']
alt-0 = ['workspace 10', 'move-mouse monitor-lazy-center']

# --- Move current window to workspace N ---
alt-shift-1 = 'move-node-to-workspace 1'
alt-shift-2 = 'move-node-to-workspace 2'
alt-shift-3 = 'move-node-to-workspace 3'
alt-shift-4 = 'move-node-to-workspace 4'
alt-shift-5 = 'move-node-to-workspace 5'
alt-shift-6 = 'move-node-to-workspace 6'
alt-shift-7 = 'move-node-to-workspace 7'
alt-shift-8 = 'move-node-to-workspace 8'
alt-shift-9 = 'move-node-to-workspace 9'
alt-shift-0 = 'move-node-to-workspace 10'

# --- Workspace "back and forth" (like tmux last-window) ---
alt-tab = 'workspace-back-and-forth'

# --- Monitor navigation ---
#   Alt+[        → focus LEFT external  (workspace 10, pinned to HP 327se (2))
#   Alt+]        → focus RIGHT external (workspace 9,  pinned to HP 327se (1))
#   Alt+Enter    → home (laptop — always goes to laptop's currently-visible workspace)
#   Alt+Shift+[  → send focused window to LEFT external + follow
#   Alt+Shift+]  → send focused window to RIGHT external + follow
alt-leftSquareBracket        = ['workspace 10', 'move-mouse monitor-lazy-center']
alt-rightSquareBracket       = ['workspace 9',  'move-mouse monitor-lazy-center']
alt-enter                    = 'exec-and-forget ~/.config/aerospace/focus-home.sh'
alt-shift-leftSquareBracket  = ['move-node-to-workspace 10', 'workspace 10', 'move-mouse monitor-lazy-center']
alt-shift-rightSquareBracket = ['move-node-to-workspace 9',  'workspace 9',  'move-mouse monitor-lazy-center']

# --- Reload Aerospace config ---
alt-shift-c = 'reload-config'

# ==========================
# Apps that should always float (not tile)
# ==========================
[[on-window-detected]]
if.app-id = 'com.apple.systempreferences'
run = 'layout floating'

[[on-window-detected]]
if.app-id = 'com.apple.finder'
run = 'layout floating'

[[on-window-detected]]
if.app-id = 'us.zoom.xos'
run = 'layout floating'

[[on-window-detected]]
if.app-id = 'com.apple.ActivityMonitor'
run = 'layout floating'

[[on-window-detected]]
if.app-id = 'com.apple.archiveutility'
run = 'layout floating'

[[on-window-detected]]
if.app-id = 'com.1password.1password'
run = 'layout floating'

# Apps that pop up / stay on top — let them float instead of fighting tiling
[[on-window-detected]]
if.app-id = 'com.grammarly.ProjectLlama'
run = 'layout floating'

# Spotify — persistent media player window glitches when tiled across monitors
[[on-window-detected]]
if.app-id = 'com.spotify.client'
run = 'layout floating'

# Apple Music + Messages have the same "persistent floating" feel
[[on-window-detected]]
if.app-id = 'com.apple.Music'
run = 'layout floating'

[[on-window-detected]]
if.app-id = 'com.apple.MobileSMS'
run = 'layout floating'

[[on-window-detected]]
if.app-id = 'com.apple.FaceTime'
run = 'layout floating'

[[on-window-detected]]
if.app-id = 'com.grammarly.macgap'
run = 'layout floating'

[[on-window-detected]]
if.app-id = 'com.docker.docker'
run = 'layout floating'

[[on-window-detected]]
if.app-id = 'com.apple.loginwindow'
run = 'layout floating'

# Quick-terminal drop-downs and small panels
[[on-window-detected]]
if.window-title-regex-substring = '(?i)preferences'
run = 'layout floating'
